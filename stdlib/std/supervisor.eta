;;; std/supervisor.eta - Compatibility shim for std.actor.supervisor.
;;;
;;; Prefer `(import std.actor.supervisor)` for new code.

(module std.supervisor
  (export one-for-one one-for-all rest-for-one)
  (import std.actor.supervisor)
  (begin))
